<!DOCTYPE html>
<html lang="en">
<%- include('partials/head')%> 
<!-- 第一行 可以導入 views 的 partials 裡面的head.ejs -->


    <%- include('partials/navbar')%> 
    <h1>number is <%= num %></h1>
    <% if (num %2 ===0 ){ %>
    <h2>這是偶數</h2>
    <% } else { %>
    <h2>這是基數</h2>
    <% } %>
    <h3>That number is : <%=num%2 ===0 ? 'EVEN' : 'ODD' %></h3>
    <%- include('partials/footer')%> 